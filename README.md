Vitamin taking reminder written in pure Rust

## How to use
- Use `↑`,`←`,`↓`,`→` to move.
- Use `Control` + `s` to save the data.
- Use `q` ti quit.

- ## Installation
- ```
  $ cargo install vita-rs
  ```
